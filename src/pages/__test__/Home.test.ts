import renderer from "react-test-renderer";

describe("Just testing", () => {
  test("just random test", () => {
    expect(1 + 2).toBe(3);
  });
  test("just random test2", () => {
    expect(1 + 2).toBe(5);
  });
});
